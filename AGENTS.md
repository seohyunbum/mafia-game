# AGENTS.md — 마피아 게임 작업 지침

## 실행 표면 (표준)

**이 게임의 표준 실행 경로는 바탕화면 아이콘이 여는
<https://seohyunbum.github.io/mafia-game/> 이고, 그 화면은 `gh-pages` 브랜치의 정적 배포본이다.**

- 로컬 소스를 고쳐도 **배포하지 않으면 아이콘 화면은 한 글자도 바뀌지 않는다.**
- 아이콘은 로컬 파일을 읽지 않는다 — Edge 앱 모드로 위 URL 을 여는 바로가기다.
- 배포 절차는 [`README.md`](README.md) §배포 참조. 조립은 `scripts/build-pages.mjs` 가 한다.
- `gh-pages` 의 `source-commit.txt` 가 지금 서비스 중인 소스 커밋이다. 배포본과 소스가
  어긋나는지 볼 때 여기부터 확인한다.

## 구조

| 경로 | 역할 |
|---|---|
| `lib/rules/` | **규칙 정본.** 순수 함수 — 화면·AI·네트워크 의존 0, 무작위성은 주입받는다 |
| `lib/flow/` | 진행 드라이버 — 요구 계산·제출 수집·페이즈 전이·가시성 부여 |
| `lib/ai/` | 규칙 기반 AI (7역할). 외부 모델을 쓰지 않는다 |
| `app/` | 표현과 입력. 규칙을 다시 구현하지 않고 `lib/flow` API 만 쓴다 |
| `data/` | 역할·규칙 수치 정본. 해석(🟡)은 전부 여기 값으로 내려와 있다 |
| `docs/DESIGN.md` | **사용자 승인 규칙 정본.** 코드가 이 문서와 어긋나면 문서를 먼저 고친다 |
| `lib/online/` | PeerJS 전송·프로토콜·호스트 권위 세션 (옛 행동 모델 기준, 이식 대기) |
| `lib/game/` | 배포본의 옛 규칙 구현. 온라인 계층이 아직 의존한다 — 새 작업에 쓰지 않는다 |

## 불변 규칙

1. 규칙 변경은 `docs/DESIGN.md` 와 `data/*.json` 을 **먼저** 갱신한다.
2. **규칙 판정은 `lib/rules` 안에서만** 한다. `lib/flow` 와 `app` 은 판정하지 않는다.
3. **`advance()` 는 던지지 않는다.** 진행할 수 없으면 `{ok:false, blocked}` 로 이유를 돌려준다.
   화면이 받을 데 없는 예외로 흐름을 멈추게 하지 않는다 — 배포본이 무너진 방식이 그것이었다.
4. AI 몫 제출은 **진행 시점에 드라이버가 채운다.** AI 때문에 사람이 멈추지 않는다.
5. 합법 대상이 없는 제출은 `optional` 로 강등한다. 규칙이 막아서 밤이 끝나지 않는 일이 없게 한다.
6. 호스트만 전체 상태를 소유한다. 게스트에는 시야가 적용된 뷰만 보낸다.
7. 게스트 입력은 intent 로 검증하며 임의 상태 덮어쓰기를 허용하지 않는다.
8. 랜덤 판정은 시드 RNG 를 쓴다. 같은 시드는 같은 판을 만들어야 한다.
9. 외부 LLM·Firebase 를 게임 진행에 쓰지 않는다.
10. **AI 의심 점수에 "대화에서 이름이 몇 번 불렸는가" 를 넣지 않는다.** 그 항목이 사람 좌석을
    매 판 최다 득표자로 만들었다. 규칙이 만들어내는 증거만 쓴다.
11. 커밋 전 `npm run verify` 를 통과한다.

## 규칙을 바꿀 때

밸런스는 눈으로 판단하지 않는다. `tests/flow/harness.ts` 가 AI 로 판을 끝까지 돌려 준다.

```bash
node --test tests/rules/*.test.ts tests/flow/flow.test.ts
```

`tests/flow/flow.test.ts` 가 지키는 선:

- 진행이 멈춘 판 **0**, 규칙 코어가 거부한 입력 **0**, 승자 없이 끝난 판 **0**
- 세 진영 승률이 각각 **10% 이상** (배포본의 시민팀 1.7% 같은 상태로 돌아가지 않는다)
- 모든 역할이 실제로 능력을 쓴다
- 사람 좌석이 평균의 2배 넘게 지목되지 않는다

수치를 바꿨으면 `data/rules.json` 의 `_status` 주석에 **근거와 측정치**를 남긴다.
